// Provider notifications use the same generic notifications view as customers
// (it keys off the logged-in user and each notification's own link).
export { default } from '@/pages/customer/Notifications';
